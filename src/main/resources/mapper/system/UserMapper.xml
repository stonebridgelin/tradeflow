<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stonebridge.tradeflow.system.mapper.UserMapper">
    <!-- 结果映射 -->
    <resultMap id="SystemUserResultMap" type="com.stonebridge.tradeflow.system.entity.SystemUser">
        <id property="id" column="id"/>
        <result property="userName" column="user_name"/>
        <result property="nickName" column="nick_name"/>
        <result property="password" column="password"/>
        <result property="status" column="status"/>
        <result property="email" column="email"/>
        <result property="phonenumber" column="phonenumber"/>
        <result property="sex" column="sex"/>
        <result property="avatar" column="avatar"/>
        <result property="userType" column="user_type"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="delFlag" column="del_flag"/>
    </resultMap>

    <!-- 自定义查询：根据用户名查询用户 -->
    <select id="selectByUserName" resultMap="SystemUserResultMap">
        SELECT id, user_name, nick_name, password, status, email, phonenumber, sex, avatar, user_type,
               create_by, create_time, update_by, update_time, del_flag
        FROM sys_user
        WHERE user_name = #{userName} AND del_flag = 0
    </select>

    <!-- 自定义插入：插入用户 -->
    <insert id="insert" parameterType="com.stonebridge.tradeflow.system.entity.SystemUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (
            user_name, nick_name, password, status, email, phonenumber, sex, avatar, user_type,
            create_by, create_time, update_by, update_time, del_flag
        ) VALUES (
                     #{userName}, #{nickName}, #{password}, #{status}, #{email}, #{phonenumber}, #{sex}, #{avatar}, #{userType},
                     #{createBy}, #{createTime}, #{updateBy}, #{updateTime}, #{delFlag}
                 )
    </insert>

    <!-- 自定义更新：根据 ID 更新用户信息 -->
    <update id="updateById" parameterType="com.stonebridge.tradeflow.system.entity.SystemUser">
        UPDATE sys_user
        <set>
            <if test="userName != null">user_name = #{userName},</if>
            <if test="nickName != null">nick_name = #{nickName},</if>
            <if test="password != null">password = #{password},</if>
            <if test="status != null">status = #{status},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phonenumber != null">phonenumber = #{phonenumber},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="userType != null">user_type = #{userType},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id} AND del_flag = 0
    </update>
</mapper>